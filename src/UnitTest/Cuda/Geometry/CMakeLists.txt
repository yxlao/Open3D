# Geometry
UNIT_TEST_CUDA(TestImageCuda ${CMAKE_PROJECT_NAME} ${OPEN3D_CUDA_MODULE}
        ${GTEST_LIBRARIES} pthread)
UNIT_TEST_CUDA(TestNNCuda ${CMAKE_PROJECT_NAME} ${OPEN3D_CUDA_MODULE}
        ${GTEST_LIBRARIES} pthread)
UNIT_TEST_CUDA(TestPointCloudCuda ${CMAKE_PROJECT_NAME} ${OPEN3D_CUDA_MODULE}
        ${GTEST_LIBRARIES} pthread)
UNIT_TEST_CUDA(TestTriangleMeshCuda ${CMAKE_PROJECT_NAME} ${OPEN3D_CUDA_MODULE}
        ${GTEST_LIBRARIES} pthread)
UNIT_TEST_CUDA(TestRGBDImageCuda ${CMAKE_PROJECT_NAME} ${OPEN3D_CUDA_MODULE}
        ${GTEST_LIBRARIES} pthread)